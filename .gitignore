/node_modules
/public/hot
/public/storage
/storage/*.key
# vendor/ is committed deliberately: the cPanel host cannot run composer,
# so the server gets dependencies via git pull.
.env
.env.backup
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/storage/logs/*.log
/error_log
/.phpunit.cache
/.DS_Store
.DS_Store
D:*
